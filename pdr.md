# Product Design Record (PDR) - Notes App

## 1. Introduzione
Questo documento funge da linea guida per la progettazione e lo sviluppo dell'app **Notes**, un'applicazione web HTML per creare e organizzare note in una struttura gerarchica di cartelle. Questo file verrà aggiornato iterativamente ad ogni nuova richiesta per mantenere traccia delle funzionalità e delle specifiche dell'app.

L'app implementa la **persistenza automatica dello stato** tramite salvataggio offline locale su **IndexedDB** (`NotesAppLocalDB`), un database locale del browser completamente separato dalla cache del browser e dai dati di Google. Questo garantisce che le note e le cartelle persistano in modo affidabile anche quando l'utente cancella la cache del browser o i dati di navigazione. Il `localStorage` viene utilizzato esclusivamente per le impostazioni leggere dell'app (tema, token OAuth, profilo utente), non per i dati delle note. Al primo avvio dopo l'aggiornamento, l'app migra automaticamente eventuali dati presenti nel vecchio `localStorage` verso IndexedDB, rimuovendoli poi dal `localStorage` per completare la transizione.

## 2. Stile Visivo
- **Stile**: Minimal ed elegante, estetica pulita e raffinata.
- **Sfondo**: Nero puro (`#000000`) — ottimizzato per display AMOLED.
- **Bordi**: Bianchi/grigi chiari (`rgba(255,255,255,0.12)`) per i box e gli elementi interattivi.
- **Tipografia**: Font "Inter" da Google Fonts — pulito, moderno e leggibile (dimensione testo dell'editor impostata a `0.90rem` per massimizzare visibilità e minimalismo).
- **Palette colori**: Monocromatica in bianco e nero con accenti minimali (il colore di **ogni elemento originariamente bianco**, come icone, bottoni FAB, sfondi dei modali e titoli, viene modificato dinamicamente in base al tema scelto nelle impostazioni).
- **Icone**: SVG line-art che ereditano il colore dell'accento del tema, stile minimal e coerente.
- **Animazioni**: Transizioni fluide e sottili per navigazione, apertura note e interazioni.
- **Toggle Switch**: Il pulsante di commutazione (es. AMOLED) utilizza una pallina bianca semitrasparente (`rgba(255,255,255,0.7)`) nello stato spento e una pallina del colore dello sfondo elevato (`--bg-elevated`) nello stato acceso, garantendo contrasto visivo con qualsiasi colore accento, incluso il bianco.

## 3. Struttura e Navigazione
L'app è basata su una **struttura gerarchica ad albero**:

1. **Homepage (Cartella Madre)**: La schermata principale funge da cartella radice. Mostra tutti gli elementi (cartelle e note) al primo livello.
2. **Cartelle**: Ogni cartella può contenere sottocartelle e/o note, senza limiti di profondità.
3. **Note**: Elementi di testo semplice, apribili in modalità di scrittura a schermo intero.
4. **Navigazione Breadcrumb**: In alto viene mostrato il percorso corrente (Home > Cartella > Sottocartella) per orientarsi nella gerarchia. Ogni segmento è cliccabile per tornare rapidamente a quel livello.
5. **Navigazione ad Albero con History API**: Ogni azione di navigazione (apertura cartella, nota, impostazioni, ecc.) inserisce uno stato nella cronologia del browser (`history.pushState`). Il pulsante indietro (sia in-app che fisico Android) fa `history.back()`, che torna esattamente di un livello alla volta lungo il percorso esatto seguito dall'utente, fino alla homepage. Solo dalla homepage il tasto indietro chiude l'app. È importante notare che, per garantire un layout pulito, quando l'icona indietro scompare dalla homepage, il titolo principale "Notes" collassa a sinistra per allinearsi perfettamente con l'inizio della breadcrumb sottostante.
6. **Ricerca Globale**: Nella Top Bar, a sinistra dell'icona Impostazioni, è presente un pulsante lente di ingrandimento per aprire la ricerca universale.

## 4. Creazione di Elementi
- **Pulsanti "+"**: In basso a destra sono presenti due pulsanti flottanti (FAB) impilati verticalmente:
  - **Cartella (sopra)**: Pulsante con l'icona della cartella e un piccolo "+" in basso a destra. Al click apre un popup minimale per inserire il nome della nuova cartella.
  - **Nota (sotto)**: Pulsante con l'icona del file e un piccolo "+" in basso a destra. Al click apre direttamente l'editor a schermo intero. L'editor presenta un campo per il titolo in alto e un'area per il testo sotto. Se si crea una nuova nota, il cursore viene posizionato subito sul titolo (aprendo la tastiera). Se si apre una nota esistente, l'editor si apre senza attivare automaticamente la tastiera.

## 5. Editor di Nota (Schermo Intero)
- **Stile Blocco Note**: L'editor occupa l'intero schermo del dispositivo. Ha un input dedicato per il titolo all'inizio e un'area di testo sotto. Nessun contorno o box visibile. All'apertura di una nota, l'editor viene sempre posizionato in cima (scroll to top).
- **Area di Scrittura**: Un'area verticale con sfondo nero e testo bianco, font Inter. L'area si auto-espande verticalmente all'inserimento del testo in modo da spingere gli eventuali allegati sotto tutto il contenuto testuale e consentire uno scroll naturale con la pagina madre.
- **Autofocus**: Nelle nuove note, il titolo prende il focus all'apertura. Per le vecchie note, la tastiera resta chiusa all'apertura finché l'utente non tocca uno dei campi.
- **Salvataggio Automatico**: Ogni modifica al testo o al titolo viene salvata in tempo reale.
- **Barra Superiore Minima**: Contiene il pulsante freccia indietro (←) per tornare alla cartella corrente, un pulsante toggle per la modalità lista, e un pulsante graffetta (📎) per allegare file.
- **Allegati File**:
  - Premendo l'icona graffetta in alto a destra si apre il file picker del dispositivo (supporto file multipli).
  - I file vengono salvati come base64 nell'array `note.attachments` in IndexedDB.
  - In fondo alla nota (scrollando dopo il testo), compare una sezione "File caricati" con bordo superiore separatore.
  - Le **immagini** vengono mostrate come anteprima visiva (max 300px di altezza, object-fit contain) con il nome del file sotto.
  - Gli **altri file** vengono mostrati come blocchetti con icona documento, nome del file e estensione/dimensione.
  - Ogni allegato ha un pulsante ✕ per eliminarlo singolarmente.
- **Conteggio Caratteri**: In basso a destra, un piccolissimo conteggio discreto dei caratteri totali.
- **Formattazione e Link**: Testo puro (plain text), senza formattazione ricca. L'editor supporta tuttavia l'evidenziazione visiva e il rilevamento dinamico degli URL: i link scritti vengono sottolineati automaticamente. Effettuando un singolo "tap" su un collegamento (che non deve essere confuso con lo scroll della pagina), apparirà immediatamente un menu fluttuante contestuale con il pulsante "Apri link" per visitarlo in una nuova scheda, senza perdere la pulizia del plain text.
- **Modalità Lista/Checklist**:
  - Tramite l'icona "quadrato con spunta" in alto a destra, l'editor converte ogni riga di testo in un elemento di una lista interattiva.
  - **Sostituzione Completa della Vista**: Quando si passa dalla modalità nota alla modalità lista (e viceversa), l'intera sezione precedente viene nascosta e sostituita dalla nuova. In particolare, il contenitore della textarea (incluso il backdrop per l'evidenziazione URL) viene completamente rimosso dalla visualizzazione quando si attiva la modalità lista, impedendo che il testo della nota rimanga visibile in cima con la checklist in basso.
  - L'icona in alto cambia in una "matitina", che se premuta riporta alla modalità di testo semplice (ripristina la nota unendo le righe).
  - Ogni riga della lista presenta: un quadratino spuntabile a sinistra, il testo editabile della riga, e l'icona del cestino a destra per cancellarla.
  - Spuntando un elemento, questo viene disattivato (testo sbarrato, minore opacità) e spostato in una sezione inferiore "Completato".
  - Tenendo premuta una riga (incluso il testo interno, ad eccezione dei pulsanti specifici come checkbox o cestino) è possibile trascinarla in su o giù per riordinarla rispetto alle altre righe attive.
  - È possibile aggiungere nuove righe rapidamente tramite il campo testuale dedicato posizionato dopo l'ultimo elemento attivo ma prima della sezione di quelli completati.

## 6. Visualizzazione Elementi nella Lista
- **Ordinamento Personalizzabile**: Le cartelle vengono prima, seguite dalle note. Nelle "Impostazioni" l'utente può scegliere il criterio di ordinamento dell'intera lista tra: "Alfabetico (Predefinito)", "Ultima Modifica", e "Data di Creazione", tramite un menu a tendina (`<select>`) elegante e minimale.
- **Fissa in alto (Pinning)**: Gli elementi marcati come "fissati" appariranno sempre in cima al proprio gruppo (prima le cartelle fissate in cima, poi le note fissate), indipendentemente dal criterio di ordinamento globale.
- **Cartelle**: Mostrate con un'icona cartella SVG minimal, nome a destra, e un'icona a forma di "matita" per rinominarle rapidamente tramite popup.
- **Note**: Mostrate con un'icona documento SVG minimal, titolo a destra, con un'anteprima del testo sotto in grigio e data dell'ultima modifica.

## 7. Azioni sugli Elementi e Meccaniche Cestino
- **Gestione Touch Intelligente**: Il sistema distingue in modo robusto tre gesti:
  - **Tap (tocco e breve rilascio)**: Apre l'elemento (cartella o nota). Per prevenire aperture accidentali durante lo scorrimento, il tap viene annullato in caso di spostamento del dito superiore a 15px o se interrotto da uno scroll nativo del browser (`pointercancel`).
  - **Movimento verticale (scroll)**: Scorre nativamente la lista delle note.
  - **Movimento orizzontale (swipe)**: Elimina l'elemento. La cattura del pointer ("pointer capture") avviene solo quando viene confermato un movimento laterale intenzionale, permettendo nel resto dei casi uno scorrimento fluido e naturale della lista.
  - **Pressione Prolungata (Long Press)**: Mantenendo la pressione (circa 500ms) su un elemento o una cartella senza spostamenti in pixel, si attiva il menu contestuale, segnalato da un breve feedback aptico. Il menu emerge in sovrimpressione permettendo azioni aggiuntive come "Rinomina" (solo cartelle), "Fissa in alto / Rimuovi da in alto", "Scarica", "Condividi" ed "Elimina". Tutte le opzioni presentano etichette testuali esplicite.
    - L'azione **Condividi** sfrutta le API native di sistema permettendo di inoltrare direttamente il testo della nota ad app di messaggistica ed email.
    - L'azione **Scarica** esporta localmente i contenuti testuali. Le singole Note vengono salvate come `.txt` leggibili pur formattando idoneamente le checklist, se presenti. Se l'azione interviene su una cartella, tutti i suoi contenuti (ed eventuali sotto-cartelle annidate) vengono rielaborati in file `.txt` poi raggruppati internamente al browser e smistati localmente verso il dispositivo con un archivio `.zip`.
- **Tap su Cartella**: Naviga dentro la cartella.
- **Tap su Nota**: Apre l'editor a schermo intero.
- **Swipe Laterale Orizzontale**: Scorrendo su un elemento (cartella o nota) a destra o sinistra, la card si sposta rivelando lo sfondo scuro dell'app con l'icona del cestino nel colore del tema. Lo sfondo dello swipe è trasparente per permettere di vedere chiaramente la card che scorre. L'icona si ingrandisce fluidamente in base allo scorrimento (scaling progressivo da 0.8 a 1.3). Oltrepassata la soglia di 80px, l'elemento viene eliminato con un'animazione fluida di contrazione.
- **Cestino e Impostazioni**: L'accesso al cestino avviene tramite l'icona ingranaggio (Impostazioni) in alto a destra. All'interno delle impostazioni è presente la sezione "Cestino" dove è possibile ripristinare o eliminare definitivamente gli elementi. Gli elementi nel cestino sono automaticamente ordinati in data decrescente mostrando per primi quelli eliminati più di recente.
- **Dati Backup**: La card "Dati Backup" nelle impostazioni contiene esclusivamente due pulsanti: **Esporta** (con icona download ↓) per esportare tutti i dati in formato `.json`, e **Importa** (con icona upload ↑) per importare un file `.json` precedentemente esportato. Nessun'altra scritta o azione è presente nella card.
- **Robustezza DOM**: Tutti i riferimenti a elementi opzionali dell'interfaccia (come pulsanti che potrebbero non essere presenti nel DOM) vengono verificati con un null-check prima di aggiungere event listener, per evitare che un elemento mancante causi un crash dello script che blocchi l'esecuzione di tutto il codice successivo (inclusa l'autenticazione Google).

## 8. Profilo Google & Sincronizzazione Drive
- **Sezione Cloud Sync nelle Impostazioni**: Card dedicata nelle impostazioni con un design estremamente minimal ed elegante:
  - Stato disconnesso: Un singolo pulsante "Connetti account Google" con logo Google minimale.
  - Stato connesso: Una vista compatta che mostra l'avatar circolare dell'utente, il nome, e lo stato di sincronizzazione in tempo reale. A destra, due pulsanti circolari minimali (icone) permettono rispettivamente di attivare la sincronizzazione manuale e di disconnettere l'account.
- **Autenticazione**: Utilizza Google Identity Services (GIS) con OAuth 2.0 Token Model. Scope richiesti: `drive.appdata`, `userinfo.profile`, `userinfo.email`.
- **Storage su Drive**: I dati vengono salvati come file JSON (`notes_app_data.json`) nella cartella `appDataFolder` di Google Drive (nascosta all'utente, riservata all'app).
- **Rilevamento Conflitti alla Connessione (Always-Ask)**: Quando l'utente collega l'account Google (o si riconnette dopo una disconnessione), l'app verifica subito la situazione. Se **almeno uno dei due lati** (locale o cloud) contiene dati, il dialogo di conflitto viene **sempre mostrato** all'utente, senza eccezioni e senza shortcut automatici, chiedendo se vuole "Sovrascrivi dati locali" (scarica dal cloud) o "Sovrascrivi i dati online" (carica locali sul cloud). L'unico caso in cui il dialogo non compare è quando entrambi i lati sono vuoti.
- **Strategia di Merge (sync successive)**: Per le sincronizzazioni automatiche successive alla prima, viene utilizzata la funzione condivisa `mergeItems()` che confronta `updatedAt` per ogni elemento: vince il più recente tra locale e cloud. La cancellazione di un elemento (soft delete con `deleted: true`) aggiorna anche `updatedAt`, garantendo che la versione locale cancellata sia riconosciuta come più recente rispetto a quella cloud non cancellata e venga correttamente propagata. La logica di merge è stata estratta in una singola funzione condivisa, eliminando la duplicazione tra `performStartupSync()` e `syncWithDrive()`.
- **Auto-sync Silenziosa (Token Persistence + Invisible Refresh)**: All'avvio dell'app, il token di accesso OAuth viene cercato prima in `localStorage` (dove è persistito insieme al suo timestamp di scadenza). Se il token è ancora valido, viene usato immediatamente per avviare la sincronizzazione in background senza alcuna interazione visibile. Se il token risulta scaduto (es. l'app viene riaperta il giorno dopo), il sistema utilizza `gapi.auth.authorize` con `immediate: true` per ottenere un nuovo token in modo completamente invisibile tramite un **iframe nascosto** gestito internamente dalla libreria GAPI. Se il refresh silenzioso fallisce, l'app indica solo "Tocca Sincronizza per aggiornare" nelle impostazioni. Per i salvataggi, l'app utilizza una strategia di **Fast Sync** con un **unico timer unificato** (`fastSyncTimer`): ogni modifica innesca un upload diretto `PATCH` dopo soli **500ms**, senza effettuare operazioni costose di lettura + merge. La procedura `syncOnClose()` subentra solo come backup congiunto a `Service Worker`.
- **Sync Manuale Ottimizzata (Zero-Latency)**: Disponibile nelle impostazioni tramite il pulsante "Sincronizza ora", con feedback visivo. I tempi di connessione e sincronizzazione sono stati pesantemente ottimizzati per essere quasi istantanei, aggirando le normali latenze delle API Google:
  - **Verifica Token Locale**: L'app si assicura della validità del token leggendo istantaneamente il timestamp di scadenza dal `localStorage`, eliminando del tutto le lente richieste di rete all'endpoint di verifica `tokeninfo` di Google.
  - **Cache ID File Drive**: L'ID del file di backup su Drive (`driveFileId`) viene anch'esso cachato in RAM e persistito in `localStorage`. Questo elimina la necessità di interrogare e ricercare il file ad ogni singola iterazione di sync o all'avvio dell'app, salvando un'ulteriore chiamata di rete.
- **Configurazione**: Utilizza il `GOOGLE_CLIENT_ID` specifico del progetto su Google Cloud Console (configurato in `app.js`). L'app non verificata supporta fino a 100 utenti test configurati nella console.

## 9. Gestione PWA e Aggiornamenti
L'applicazione è configurata come **Progressive Web App (PWA)** per garantire il funzionamento offline e un'esperienza simile a un'app nativa:
- **Service Worker (sw.js)**: Utilizza una strategia **Network-First** rigorosa (`cache: 'no-store'`). Previene il caricamento di file vecchi e stantii dalla cache HTTP del browser scaricando e aggiornando istantaneamente l'ultima versione disponibile ad ogni avvio online (gestendo contemporaneamente il fallback offline aggiornando iterativamente la cache con ogni risorsa scaricata).
- **Aggiornamenti Automatici**: All'apertura dell'app, il Service Worker controlla immediatamente se esiste una nuova versione sul server. Viene inoltre eseguito un controllo periodico ogni 10 minuti.
- **Auto-Reload**: Quando viene scaricato un nuovo Service Worker, il codice in `app.js` rileva il cambiamento di controllo (`controllerchange`) e ricarica automaticamente la pagina per applicare istantaneamente le ultime modifiche senza intervento manuale dell'utente (come la cancellazione dei dati del browser). Il listener viene attivato solo se esisteva già un controller attivo (`hadController`), evitando un reload inutile alla primissima visita dell'utente.
- **skipWaiting & clients.claim**: Il Service Worker è configurato per attivarsi immediatamente e prendere il controllo della pagina non appena scaricato, velocizzando il processo di aggiornamento.

## 10. Ottimizzazioni Architetturali e Performance
- **Conteggio Figli On-the-Fly (Rimozione Complessità $O(N^2)$)**: Per evitare i classici rallentamenti di rendering (`renderItems()`) in presenza di centinaia di note, e assecondando la richiesta di mantenere la gestione dell'app "stateless" (senza meccanismi di cache prolungata), il calcolo degli elementi interni di ogni determinata cartella avviene ora on-the-fly tramite un singolo conteggio matriciale passante `$O(N)$` che intercetta i nodi e previene calcoli esponenziali in profondità per la visualizzazione dell'interfaccia.
- **Unified Multi-Sorting Ponderato (Sort $O(N \log N)$ singolo)**: Anziché dividere 4 volte tutti gli elementi per filtrare (`.filter()`) e di ordinare ciascun frammento generato (`.sort()`) concatenandoli in chiusura (`.concat()`), la lista (tra cartelle pinnate, appunti, normali e sfissi) viene ora elaborata con un'unica passata di `.sort()`. Implementando una priorità a gerarchie (`4, 3, 2, 1` per fissato vs unfissato) il carico sul garbage collector e il calcolo complessivo viene decurtato del ~75%.
- **Lookup con Indicizzazione Dinamica $O(1)$**: La ricerca di una precisa nota per ID (`getItem()`) passa da un costo lineare ad uno costante istantaneo. Rispettando il vincolo di scartare cache durature inter-sessione che rischiano di disallinearsi con IndexedDB o Google Drive, l'app avvolge la ricerca locale tramite un Dictionary Proxy (`Map`). Quest'ultimo si invalida e autogenera silente al volo unicamente rilevando mutamento di reference memory o difformità di length globale dell'albero.
- **Mitigazione Memory Spike su Parsing Testi**: Precedentemente, l'estrazione dell'anteprima testuale per la generazione delle note in lista convertiva l'intero contenuto di ciascun appunto in array mostruosi in memoria tramite chiamate multiple a `split('\n')`. Per documenti di migliaia di righe questo innescava lag fatali ai dispositivi mobile. Ora il consumo RAM viene isolato processando esclusivamente una _micro-slice string passiva_ di soli 150 caratteri estratta tramite limitatori `indexOf`, mantenendo stabile e asintotico il dispendio risorse.
- **Delete Recursivo Flat (Rimozione lag in O(N^2))**: L'algoritmo originario della funzione `deleteRecursive()` clonava l'array scartando gli item e avviava una ricorsione distruggendo progressivamente variabili figlie; in caso di cartelle molto profonde innescava centinaia di chiamate e duplicazioni O(N^2). È stato riconvertito ad un pattern iterativo piatto (`Iterative Flat Sets`): gli alias vengono marcati cumulativamente in una Set List temporanea, per avviare la frammentazione e il filtraggio nativo del cestino **un'unica volta** allo svuotamento generale, abbattendo radicalmente il computational loop.
- **Prevenzione Layout Thrashing (DocumentFragment)**: Qualsiasi istruzione ciclica che altera l'integrità del DOM, come `renderItems()` o `renderTrashList()`, inseriva precedentemente i vari elementi html uno per volta tramite un `appendChild` asincrono posto nel ciclo ripetuto in blocco. Questa imprecisione causava ricalcoli strutturali dello schermo (Reflow e Repaint individuali) per ogni elemento scatenando stuttering ad alta concentrazione di appunti. Questo difetto è stato epurato tramite un inserimento astratto delle card virtuali su instanza in-memory offscreen di `DocumentFragment`, iniettando l'intero set visivo in un'unica frazione di loop pre-assemblata nella view finale azzerando la disconnessione grafica.
- **Upload Google Drive "Media" (Riduzione Payload)**: Precedentemente, la routine di caricamento `writeDriveFile` verso le Google APIs trasmetteva il nuovo schema locale codificandovi in ogni `PATCH` non solo il corpo ma anche i metadati superflui e i relativi separatori testuali di pacchetto in formato `multipart/related`. Passando a un formato crudo `uploadType=media` nei salvataggi in background (dopo la prima inizializzazione), l'applicazione spedisce esclusivamente il JSON isolato accorciando drasticamente la grandezza della richiesta ed attenuando i colli di bottiglia network.
- **Smart Date Merge Senza Parsing**: Nel modulo di sincronizzazione (`syncWithDrive`), il motore decreta l'autorità conflittuale fra backup cloud e storage locale calcolando le timestamp. Originariamente veniva istanziato ciecamente in un loop iterativo il costruttore `new Date()` per ogni alias. Adesso, poiché i metadati seguono un rigoroso standard stringa ordinabile (`ISO 8601`), sono stati impiantati dei calcoli sminuiti a banalissimi confronti lessicografici crudi nativi, neutralizzando il blocco RAM che comportava l'interpretazione ad oggetti.
- **Disinnesco Busy-Wait (Sync Scheduler)**: In assenza di connessione stabile, se un salvataggio si dilatava nel tempo, il trigger ritardato di 500ms scatenava nativamente un loop circolare continuo per ri-accodare il nuovo pacchetto fallendo all'infinito e affaticando i limitati core dei processori mobile. Il workaround del re-schedule infinito è stato disinnescato tramite una *Flag Chain Promise*, in grado di ignorare i richiami e lasciare riposare la CPU finché la rete non viene sbloccata dal primo payload, accodando e sovrascrivendo unicamente l'ultimo backup aggiornato in cascata pulita.
- **Connessioni TCP "Keep-Alive"**: Integrato il parametro `{ keepalive: true }` all'interno degli instradatori generici per le fetch verso Drive. Permette al browser Chromium/Safari di preservare e non terminare i tunnelling sicuri ed aprire porte addizionali dopo le transizioni di pacchetto verso le API Google, sorpassando la costosa fase di hand-shake crittografico (SSL/TLS) e validazione handshake ad ogni minuscolo editing ricorrente dell'utente.
- **Zero-Latency Avatar Rendering (Base64 Mapping)**: Durante l'autenticazione a Google, l'app scaricava temporaneamente il link dell'immagine avatar tramite URL, che creava flash vuoti e rallentamenti dell'UI ad ogni sessione offline. Implementata una pipeline logica di conversione in `Base64` ad alta efficienza (`<canvas>`) per incastonarla e archiviarla in stringa pura in localStorage alla nascita permettendo avvii a costo zero ascritti.
- **Multithreading Manual-Login**: Eliminato l'accodamento in stallo generato in UI dal recupero progressivo della callback utente su Google pre-sync. E' ora operativo uno scaricatore `Promise / Timeout 0` multithread, paralellizzando i layer operativi offrendo interazioni lampo.
- **Evadimento Blind Polling (On-Load Check)**: Distrutto il `setTimeout` artificiale da 500ms introdotto precedentemente per agganciare in delay le librerie GIS. L'algoritmo scala in logica "Reattiva" rintracciando la finalizzazione nativa del payload Google DOM.
- **Predictive Auto-Refresh Tokens**: Il Frontend ora sfrutta calcoli di logica predittiva localizzata pre-identificando scadenze dei timeout Google 5 minuti esatti prima dell'ipotetica morte della key in sessione per auto-rifornirsi di un `silent_token` pulito; tutto ciò è celato ai meccanismi UI, per evitare di paralizzare improvvisamente gli utenti durante la sync dei file.
- **EnsureToken Resiliente con Auto-Refresh**: La funzione `ensureToken()` è stata potenziata per tentare automaticamente un refresh silenzioso del token (via `silentRefreshViaGapi`) quando il token risulta scaduto, prima di propagare l'errore. Questo elimina la maggior parte dei fallimenti di sincronizzazione dovuti a token scaduti, recuperando la sessione in modo completamente invisibile all'utente.
- **Rilevamento Errori HTTP su Drive**: La funzione `driveFetch()` ora verifica lo stato HTTP della risposta (`response.ok`) e genera un errore specifico con codice di stato per le risposte non riuscite (401, 403, 500, ecc.). Questo permette al sistema di distinguere errori di autenticazione da altri tipi di errori e attivare la logica di retry appropriata.
- **Retry Automatico Fast Sync su Errori Auth**: Quando il Fast Sync rileva un errore HTTP 401 o 403 (token scaduto/revocato), il sistema tenta automaticamente un refresh del token tramite `ensureToken()` e rilancia la sincronizzazione. Solo se il refresh fallisce viene mostrato un messaggio di "Sessione scaduta" all'utente.
- **Pre-load GAPI Auth Istantaneo**: Il modulo `gapi.auth` viene ora pre-caricato immediatamente al caricamento della pagina (in parallelo con il rendering UI), anziché essere scaricato on-demand al primo token refresh. Questo elimina ~2-5 secondi di latenza di rete alla prima operazione di refresh. Rimosso anche il `setTimeout(initGoogleAuth, 300)` a favore di inizializzazione immediata event-driven. Timeout di autenticazione ridotto da 8s a 5s poiché il modulo è già in memoria.
- **Init Google Auth con Polling Resiliente**: Risolto il bug critico che impediva l'auto-sincronizzazione all'avvio. La funzione `initGoogleAuth()` ora utilizza un meccanismo di **polling a fallback** (ogni 200ms, fino a 15 secondi) oltre al classico hook sull'evento `load` dello script Google. Questo risolve la race condition in cui l'evento `load` veniva perso perché lo script si caricava prima che il listener fosse registrato, lasciando l'auth bloccata permanentemente.
- **driveFetch con Retry Auth Automatico**: La funzione `driveFetch()` ora intercetta automaticamente errori HTTP 401/403 e tenta un **refresh silenzioso del token + retry** della richiesta originale prima di propagare l'errore. Inoltre, i messaggi di errore tecnici come "Failed to fetch" vengono tradotti in italiano user-friendly ("Errore di rete. Controlla la connessione.").
- **Diagnostica Sync "More Details"**: Aggiunto un menu a tendina collassabile "More details" all'interno della card Google nelle impostazioni. Quando espanso, mostra un log diagnostico in tempo reale con gli eventi del ciclo di vita della sincronizzazione (caricamento libreria, ripristino token, refresh silenzioso, avvio sync, completamento, errori). Ogni voce è color-coded (verde per successo, rosso per errori, grigio per info) con timestamp. Il log mantiene gli ultimi 30 eventi in memoria e si aggiorna automaticamente ad ogni evento. Implementato tramite `logSyncEvent()` e `renderSyncLog()` con toggle UI via `toggleSyncDetails()`.
- **Resilienza preloadGapiAuth**: Corretto un bug critico in cui `preloadGapiAuth()` cachava una Promise rifiutata permanentemente se il primo tentativo falliva (es. script GAPI caricato in ritardo). Ora il campo `_gapiAuthReady` viene resettato a `null` in caso di errore, permettendo tentativi successivi di ricaricamento del modulo auth.
- **Fix Retry driveFetch con Header Aggiornato**: Corretto un bug in cui il retry dopo un errore 401/403 riutilizzava l'oggetto `options` originale contenente ancora il vecchio token nell'header `Authorization`. Ora viene creato un nuovo oggetto options con il token fresco prima del retry, eliminando loop infiniti di autenticazione fallita.
- **Recupero Automatico File 404**: La funzione `readDriveFile()` ora intercetta errori HTTP 404 (file cancellato o account cambiato) e resetta automaticamente il `driveFileId` cachato e il relativo `localStorage`, permettendo alla successiva sync di ri-scoprire il file corretto tramite `findDriveFile()`.
- **Recupero Automatico File 404 in writeDriveFile**: Corretto un bug dove, se il file remoto veniva eliminato mentre l'app era in funzione, i Fast Sync successivi (eseguiti in PATCH) fallivano permanentemente con errore 404. Ora in caso di 404 resetta il cache ID obsoleto ed effettua un fallback automatico alla creazione del file (POST) senza perdere dati.
- **Auto-Recovery sul Refresh Token Preventivo**: Precedentemente, se il timer di auto-rinnovo del token (`schedulePredictiveTokenRefresh`) falliva per momentanea assenza di rete, interrompeva silenziosamente la sua catena di ricorsione, lasciando scadere irrevocabilmente la sessione. Ora traccia l'errore nel Diagnostic Log e ri-schedulata ricorsivamente un nuovo retry cautelare dopo 60 secondi salvando la stabilità della connessione.
